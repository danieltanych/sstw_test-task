# SSTW Test Task API

REST API для управління роботодавцями, вакансіями та працівниками, розроблений на NestJS з використанням Prisma ORM та PostgreSQL.

## 🛠 Технології

- **NestJS** v10.x - прогресивний Node.js фреймворк
- **Prisma** v7.x - сучасна ORM для TypeScript
- **PostgreSQL** - реляційна база даних (Neon.tech)
- **TypeScript** v5.x - типізована надбудова над JavaScript
- **Swagger/OpenAPI** - автоматична документація API
- **class-validator** - валідація даних
- **Jest** - фреймворк для тестування
- **ESLint** - лінтер для якості коду

## 📋 Вимоги

- **Node.js**: v22.17.0 або вище
- **npm**: v10.x або вище

## 🚀 Встановлення та запуск

### 1. Клонування репозиторію та встановлення залежностей

```bash
# Встановлення Node.js через nvm (якщо потрібно)
nvm install 22.17.0
nvm use 22.17.0

# Встановлення залежностей
npm install / npm ci
```

### 2. Налаштування бази даних

Налаштування бази захардкоджені в .env файлі
```

### 3. Міграція бази даних

```bash
# Застосування міграцій
npx prisma migrate deploy

# Генерація Prisma Client
npx prisma generate
```

### 4. Запуск додатку

```bash
# Development режим з hot-reload
npx prisma generate && npm run start:dev
```

Додаток буде доступний за адресою: **http://localhost:3000**

Swagger документація: **http://localhost:3000/api**

## 🔐 Аутентифікація

Всі ендпоінти захищені хардкод Bearer токеном.

**Токен для доступу:**
```
sstw-secret-token-2024-nestjs-api
```

### Використання у Swagger:
1. Відкрийте http://localhost:3000/api
2. Натисніть кнопку **"Authorize"** (іконка замка)
3. Введіть токен: `sstw-secret-token-2024-nestjs-api`
4. Натисніть "Authorize"

### Використання у запитах:
Додайте заголовок до кожного запиту:
```
Authorization: Bearer sstw-secret-token-2024-nestjs-api
```

Приклад curl:
```bash
curl -H "Authorization: Bearer sstw-secret-token-2024-nestjs-api" \
  http://localhost:3000/health
```

## 🧪 Тестування

```bash
# Запуск юніт тестів
npm test

# Запуск тестів у watch режимі
npm run test:watch

# Запуск тестів з покриттям коду
npm run test:cov

# Запуск e2e тестів
npm run test:e2e
```

## 🔍 Лінтинг

```bash
# Перевірка коду
npm run lint

# Автоматичне виправлення помилок
npm run lint -- --fix
```

## 📊 Структура бази даних

### Таблиці

#### 1. **Employer** (Роботодавці)
Зберігає інформацію про компанії-роботодавців.

| Поле | Тип | Опис |
|------|-----|------|
| `id` | Int | Унікальний ідентифікатор (auto-increment) |
| `name` | String | Назва компанії |
| `status` | EmployerStatus | Статус роботодавця (active/inactive) |
| `createdAt` | DateTime | Дата створення |
| `updatedAt` | DateTime | Дата оновлення |

**Зв'язки:**
- `jobs[]` - вакансії роботодавця (one-to-many)
- `workers[]` - працівники роботодавця (one-to-many)

---

#### 2. **Job** (Вакансії)
Зберігає інформацію про вакансії.

| Поле | Тип | Опис |
|------|-----|------|
| `id` | Int | Унікальний ідентифікатор (auto-increment) |
| `name` | String | Назва вакансії |
| `status` | JobStatus | Статус вакансії (draft/active/archive) |
| `creationDate` | DateTime | Дата створення вакансії |
| `salary` | Decimal | Зарплата по вакансії |
| `employerId` | Int | ID роботодавця (foreign key) |
| `createdAt` | DateTime | Дата створення запису |
| `updatedAt` | DateTime | Дата оновлення |

**Зв'язки:**
- `employer` - роботодавець (many-to-one)
- `workers[]` - працівники на вакансії (one-to-many)
- `workerHistory[]` - історія працівників (one-to-many)

**Каскадне видалення:** При видаленні роботодавця всі його вакансії видаляються (`onDelete: Cascade`)

---

#### 3. **Worker** (Працівники)
Зберігає інформацію про працівників.

| Поле | Тип | Опис |
|------|-----|------|
| `id` | Int | Унікальний ідентифікатор (auto-increment) |
| `name` | String | Ім'я працівника |
| `salary` | Decimal | Очікувана зарплата |
| `employerId` | Int? | ID поточного роботодавця (nullable) |
| `jobId` | Int? | ID активної вакансії (nullable) |
| `createdAt` | DateTime | Дата створення |
| `updatedAt` | DateTime | Дата оновлення |

**Зв'язки:**
- `employer` - поточний роботодавець (many-to-one, optional)
- `job` - активна вакансія (many-to-one, optional)
- `history[]` - історія найму/звільнень (one-to-many)

**Каскадне видалення:** 
- При видаленні роботодавця працівники звільняються (SetNull)
- При видаленні вакансії працівники звільняються (SetNull)

---

#### 4. **WorkerHistory** (Історія працівників)
Зберігає історію найму та звільнень працівників.

| Поле | Тип | Опис |
|------|-----|------|
| `id` | Int | Унікальний ідентифікатор (auto-increment) |
| `workerId` | Int | ID працівника (foreign key) |
| `action` | String | Дія (hired/fired) |
| `date` | DateTime | Дата події (за замовчуванням now()) |
| `jobId` | Int? | ID вакансії (nullable) |
| `employerId` | Int? | ID роботодавця (nullable) |
| `jobName` | String? | Назва вакансії (денормалізація) |
| `employerName` | String? | Назва роботодавця (денормалізація) |
| `jobSalary` | Decimal? | Зарплата вакансії (денормалізація) |

**Зв'язки:**
- `worker` - працівник (many-to-one)
- `job` - вакансія (many-to-one, optional)

**Каскадне видалення:** При видаленні працівника вся його історія видаляється (`onDelete: Cascade`)

---

### Енуми

#### EmployerStatus
- `active` - активний роботодавець
- `inactive` - неактивний роботодавець

#### JobStatus
- `draft` - чернетка вакансії
- `active` - активна вакансія
- `archive` - архівована вакансія

---

## 📡 API Endpoints

### 🏢 Employers (Роботодавці)

| Метод | Endpoint | Опис |
|-------|----------|------|
| `POST` | `/employers` | Створення нового роботодавця |
| `GET` | `/employers` | Отримання списку всіх роботодавців |
| `GET` | `/employers/:id` | Отримання інформації про конкретного роботодавця |
| `PUT` | `/employers/:id` | Оновлення інформації про роботодавця |
| `DELETE` | `/employers/:id` | Видалення роботодавця (каскадне видалення вакансій) |
| `GET` | `/employers/:id/workers` | Отримання списку всіх працівників роботодавця |

---

### 💼 Jobs (Вакансії)

| Метод | Endpoint | Опис |
|-------|----------|------|
| `POST` | `/jobs` | Створення нової вакансії |
| `GET` | `/jobs` | Отримання списку всіх вакансій |
| `GET` | `/jobs/:id` | Отримання інформації про конкретну вакансію |
| `PUT` | `/jobs/:id` | Оновлення інформації про вакансію |
| `DELETE` | `/jobs/:id` | Видалення вакансії |
| `GET` | `/jobs/date-period` | Отримання вакансій за певний період (query: startDate, endDate) |
| `PUT` | `/jobs/:id/archive` | Архівування вакансії (зміна статусу на archive) |

---

### 👷 Workers (Працівники)

| Метод | Endpoint | Опис |
|-------|----------|------|
| `POST` | `/workers` | Створення нового працівника |
| `GET` | `/workers` | Отримання списку всіх працівників |
| `GET` | `/workers/:id` | Отримання інформації про конкретного працівника |
| `PUT` | `/workers/:id` | Оновлення інформації про працівника |
| `DELETE` | `/workers/:id` | Видалення працівника (з історією) |
| `GET` | `/workers/:id/matched-jobs` | Отримання активних вакансій з зарплатою ≥ очікуваної |
| `PUT` | `/workers/:id/new-employer` | Зміна роботодавця (автоматичне створення історії) |

---

### 🏥 Health

| Метод | Endpoint | Опис |
|-------|----------|------|
| `GET` | `/health` | Перевірка стану додатку та з'єднання з базою даних |

---

## 🔄 Бізнес-логіка та транзакції

### Зміна роботодавця (`PUT /workers/:id/new-employer`)

Складна операція, яка виконується в кілька кроків:

1. **Перевірка існування працівника, роботодавця та вакансії**
2. **Перевірка зв'язку** вакансії з роботодавцем
3. **Створення запису "fired"** в історії (якщо працівник був працевлаштований)
   - Зберігається: `jobId`, `employerId`, `jobName`, `employerName`, `jobSalary`
4. **Оновлення працівника** - призначення нового роботодавця та вакансії
5. **Створення запису "hired"** в історії

**Денормалізація даних:** В історії зберігаються назви та зарплата на момент події, щоб зберегти інформацію навіть після видалення вакансії чи роботодавця.

### Видалення роботодавця (`DELETE /employers/:id`)

Каскадна операція:
1. Видаляються всі вакансії роботодавця
2. Працівники звільняються (employerId та jobId встановлюються в null)
3. Історія працівників зберігається

### Архівування вакансії (`PUT /jobs/:id/archive`)

Просте оновлення статусу вакансії на `archive`. Працівники залишаються прикріпленими до вакансії.

---

## 📁 Структура проєкту

```
src/
├── auth/              # Аутентифікація (AuthGuard)
├── employers/         # Модуль роботодавців
│   ├── dto/          # Data Transfer Objects
│   ├── enums/        # Енуми (EmployerStatus)
│   ├── types/        # TypeScript типи
│   ├── employers.controller.ts
│   ├── employers.service.ts
│   └── employers.module.ts
├── jobs/             # Модуль вакансій
│   ├── dto/
│   ├── enums/        # Енуми (JobStatus)
│   ├── types/
│   ├── jobs.controller.ts
│   ├── jobs.service.ts
│   └── jobs.module.ts
├── workers/          # Модуль працівників
│   ├── dto/
│   ├── types/
│   ├── workers.controller.ts
│   ├── workers.service.ts
│   └── workers.module.ts
├── health/           # Health check модуль
│   ├── health.controller.ts
│   ├── prisma.health.ts
│   └── health.module.ts
├── prisma/           # Prisma сервіс
│   ├── prisma.service.ts
│   └── prisma.module.ts
├── app.module.ts     # Головний модуль
└── main.ts           # Точка входу

prisma/
├── schema.prisma     # Схема бази даних
└── migrations/       # Міграції

test/
└── *.spec.ts         # Юніт тести (27 тестів)
```

---

## ✅ Покриття тестами

- **Юніт тести**: 27 тестів для всіх контролерів
- **Test Suites**: 5 (AppController, EmployersController, JobsController, WorkersController, HealthController)
- **Покриття**: Всі CRUD операції та нестандартні ендпоінти

---

## 🤝 Автор

Розроблено як тестове завдання для SSTW від Даніїл Крамаренко. Сподіваюсь вам сподобається і ми зможемо попрацювати разом! З Новим Роком!

---